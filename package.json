{
  "name": "gauge-taiko-template",
  "version": "0.0.1",
  "description": "Starter template for writing JavaScript tests for Gauge",
  "scripts": {
    "gauge:run": "gauge run specs/",
    "pretest": "rm -rf logs && mkdir -p logs/screenshots",
    "test": "jest --detectOpenHandles --runInBand"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^26.6.3",
    "taiko": "^1.0.19"
  }
}
